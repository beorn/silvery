/**
 * Token resolution — resolves `$token` strings against a Theme.
 *
 * Re-exports from @silvery/style which is the canonical implementation.
 * @silvery/theme consumers can continue importing from here.
 */

export { resolveThemeColor } from "@silvery/style"
